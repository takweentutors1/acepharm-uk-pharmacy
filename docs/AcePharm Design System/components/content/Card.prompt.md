# Card & Panel

Container components for organizing content into distinct regions.

**Card**: Standard bordered container (15px radius, 24px padding).
**Panel**: Larger page-level container (22px radius, 32px padding).

Both use white background with subtle border, carrying optional className for additional styling.
