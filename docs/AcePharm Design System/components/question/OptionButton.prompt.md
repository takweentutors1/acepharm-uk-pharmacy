# Option Button

A–E choice button for question answering. Supports selected/feedback states with semantic colouring.

**States**:
- Default: border colour, hover indigo border
- Selected (unanswered): indigo border + background
- Correct: green border + background
- Incorrect: red border + background

All feedback uses border + colour + explicit text for multi-sensory compliance.
